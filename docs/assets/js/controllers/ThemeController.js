/**
 * ThemeController
 * Scaffold — global theme state.
 * Reserved for: dark mode toggle, colour scheme preferences, or
 * any site-wide state that doesn't belong to a specific component.
 *
 * Currently a no-op placeholder.
 */
export class ThemeController {
  init() {
    // Placeholder — no theme switching in v1 design.
  }
}
